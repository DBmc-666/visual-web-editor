# 测试清单 · v1.1.3（图层面板 + 内容组件）

> **测试前必做**：先 **硬刷新页面（`Ctrl + Shift + R`）**；若界面仍异常，重启 dev 服务器（关掉窗口重新双击 `启动编辑器.bat`）。
> 原因：修改 `src/stores/*.js` 这类模块时，Vite 热更新偶尔会不同步。

---

## 0. 准备工作

- [ ] 页面正常打开，无控制台报错（F12 → Console）
- [ ] 左侧出现 4 个标签：`📦 组件` / `🧩 图层` / `📄 页面` / `🧱 布局`
- [ ] 「📦 组件」里能看到 4 个分类：基础组件 / 预设表单 / 导航组件 / **内容组件**
- [ ] 「内容组件」展开后有 6 项：分隔线 / 图标 / 列表 / 表格 / 视频 / 轮播图

---

## 1. 分隔线（Divider）

**添加与渲染**
- [ ] 点击「线 分隔线」能添加到画布，显示一条水平线

**属性面板**
- [ ] 线条样式切「虚线」「点线」，画布同步变化
- [ ] 线宽改为 `3`，线条变粗
- [ ] 线条颜色（颜色选择器）改为红色
- [ ] 中间文字填「分割内容」→ 线条中间出现文字
- [ ] 文字颜色、文字大小、文字两侧留白可调
- [ ] 清空中间文字 → 恢复为一条整线

**导出**
- [ ] 导出 HTML → 样式/颜色/文字与画布一致

---

## 2. 图标（Icon）

**属性面板**
- [ ] 图标字符改为 `🚀`，画布变化
- [ ] 背景形状切「圆形」/「圆角方形」/「无背景」，底色相应变化
- [ ] 背景颜色、背景尺寸可调
- [ ] 右侧「📝 文字样式」的字号/颜色能改变图标大小与颜色

**导出**
- [ ] 导出 HTML → 图标、形状、颜色一致

---

## 3. 列表（List）— 逐项编辑 + 跳转

**逐项编辑**
- [ ] 属性面板出现「📋 列表项配置」，每一项**独立成一块**（第 1 项 / 第 2 项 …）
- [ ] 每项有两个输入框：**文字内容** 与 **跳转链接**
- [ ] 点「+ 添加列表项」→ 新增一项，画布多一行
- [ ] 点某项的「删除」→ 该项消失，其余项顺序不变
- [ ] 直接改某项文字 → 画布对应行实时更新

**跳转**
- [ ] 给第 1 项填 `#home` → 该行文字变带下划线（表示是链接）
- [ ] 给第 2 项填 `https://example.com` → 同样变链接样式
- [ ] 第 3 项留空 → 保持普通文字，无下划线
- [ ] 「链接打开方式」切「新窗口」→ 导出后链接为 `target="_blank"`
- [ ] **编辑态点击链接不会跳走**（避免编辑时误跳）；进入「预览」后点击才跳转

**其他属性**
- [ ] 列表类型切「有序列表」→ 变成 `1. 2. 3.`
- [ ] 标记符号改 `★`、颜色改绿色（仅无序列表时显示该输入框）
- [ ] 项间距调整，行距变化

**导出**
- [ ] 导出 HTML → 条目、编号、**链接可点击且能跳转**

---

## 4. 表格（Table）— 逐项编辑

**表头（按列编辑）**
- [ ] 属性面板出现「📊 表格内容配置」
- [ ] 表头区域每列一个输入框，改标题 → 画布表头实时更新
- [ ] 点某列后面的 `✖` → 该列被删除（表头与所有数据行同步少一列）
- [ ] 点「+ 加列」→ 新增一列，数据行同步多一个空单元格

**数据行（按行编辑）**
- [ ] 每行独立成一块（第 1 行 / 第 2 行 …），行内每个单元格一个输入框
- [ ] 改某个单元格 → 画布对应格子实时更新
- [ ] 点某行「删除」→ 该行消失
- [ ] 点「+ 添加数据行」→ 新增一行，单元格数量与列数一致

**样式**
- [ ] 关闭「显示表头」→ 表头消失
- [ ] 表头背景、表头文字色、边框颜色可调
- [ ] 关闭「斑马纹」→ 隔行底色消失
- [ ] 单元格内边距调整，行高变化

**导出**
- [ ] 导出 HTML → 表格结构与画布一致

---

## 5. 视频（Video）

**直链模式**
- [ ] 添加后出现黑色视频区域，带控制条，示例视频可播放
- [ ] 关闭「显示控制条」→ 控制条消失
- [ ] 勾选「自动播放」+「静音」→ 预览模式下自动播放
- [ ] 封面图地址填图片 URL → 显示封面

**iframe 模式**
- [ ] 地址类型切「iframe 嵌入地址」，填嵌入地址 → 画布显示嵌入内容
- [ ] 切回「视频直链」恢复 video 渲染

**导出**
- [ ] 导出 HTML → 视频/iframe 正常加载

---

## 6. 轮播图（Carousel）— 逐项编辑 + 跳转 + 本地图片

**逐项编辑（每张图独立一组）**
- [ ] 属性面板出现「🖼 图片与跳转配置」，每张图独立成一块（第 1 张 / 第 2 张 …）
- [ ] 每块包含：**缩略图预览** + 「📁 本地图片」按钮 + **图片地址** + **说明文字** + **跳转链接**
- [ ] 点「+ 添加图片」→ 新增一张，指示点数量增加
- [ ] 点某张「删除」→ 该张消失，其余顺序不变
- [ ] 改某张的说明文字 → 画布切换到该张时底部说明同步

**跳转**
- [ ] 给第 1 张填跳转链接 → 画布上鼠标移到图片上变成手型
- [ ] 「链接打开方式」切「新窗口」→ 导出后为 `target="_blank"`
- [ ] 第 2 张留空 → 该张不可点击
- [ ] **编辑态点击图片不会跳走**；「预览」模式下点击才跳转

**本地图片（重点）**
- [ ] 点某张的「📁 本地图片」选一张本地图片 → 缩略图立即显示该图，文件名显示在按钮旁
- [ ] 图片地址输入框显示「（已使用本地图片，导出时会一起打包）」
- [ ] 点「✖ 清除」→ 该张本地图片被清空
- [ ] 多张图片可以分别选不同的本地图片

**其他属性**
- [ ] 关闭「显示指示点」→ 圆点消失
- [ ] 关闭「显示左右箭头」→ 箭头消失
- [ ] 勾选「自动播放」+ 间隔 `1000` → 预览模式下自动切换
- [ ] 编辑态**不会**自动播放（避免干扰）

**导出（含本地图片打包）**
- [ ] 页面里含本地图片时，点「导出 HTML」→ 下载的是 **ZIP**
- [ ] 解压 ZIP：里面有 `page.html` + `images/` 文件夹
- [ ] `images/` 里有你选的本地图片（文件名与你选的原始文件名一致）
- [ ] 打开 `page.html` → **本地图片正常显示**（不是空白）
- [ ] 轮播箭头能点击切换、指示点能跳转、带链接的图能点击跳转
- [ ] 导出 Vue → 同样能跑，本地图片正常

---

## 7. 导出回归（本次改了导出代码，重点验证）

- [ ] 应用一个预设页面（如「企业介绍页」），导出 HTML → 与画布一致
- [ ] 页面里放：文本 / 图片 / 按钮 / 容器 / 链接 → 导出后全部正常
- [ ] 放「日期时间」→ 导出后时间**每秒跳动**（脚本未被破坏）
- [ ] 放「登录表单」→ 导出后表单可提交
- [ ] 同时放 日期时间 + 轮播 + 表单 + 表格 → 导出后全部正常
- [ ] 导出 Vue 工程 → `npm install && npm run dev` 能跑起来
- [ ] 图片组件（原有的）选本地图片 → 导出仍是 ZIP 且图片正常

---

## 8. 编辑器功能回归

**图层面板**
- [ ] 「🧩 图层」默认按位置排列（从上往下、大块在前），子组件缩进
- [ ] 点容器前 `▾` 能折叠/展开
- [ ] 单击 = 只选该组件；**双击** = 连同内部组件一起选中
- [ ] `👁` 隐藏 → 画布消失且导出不含它；`🔒` 锁定 → 拖不动、对齐/删除也跳过
- [ ] `✎` 改名、`✕` 删除正常
- [ ] 「按层级」模式下可拖拽调整同级顺序

**对齐与快捷键**
- [ ] 多选 ≥2 个 → 出现「🧩 多选操作」，6 种对齐都生效
- [ ] 多选 ≥3 个 → 水平/垂直等距生效（首尾不动）
- [ ] `Ctrl+C/V/D/A`、方向键微调（`Shift` 10px）、`Delete` 正常
- [ ] `Ctrl+Z` 能撤销以上操作

**草稿与 AI**
- [ ] 改动后刷新页面 → 内容自动恢复（状态栏显示「已从草稿恢复」）
- [ ] **本地图片选完后刷新页面 → 图片仍在**（草稿保存了 data URL）
- [ ] 打开「🤖 AI 生成」→ Key 文件加载 → 测试连接成功
- [ ] 「直接生成成品网页」能下载 index.html
- [ ] 「回到画布继续编辑」应用后正常，重叠被自动修复

---

## 9. 已知限制（不是 bug）

- 轮播在**编辑态不自动播放**（预览态与导出文件才播放）
- 视频「自动播放」需浏览器允许：已默认勾选「静音」，否则会被拦截
- **视频封面暂不支持选本地图片**（只支持图片地址），后续可补
- 本地图片以 data URL 形式存在页面数据里，**图片过大时草稿会变大**（建议单张控制在 1MB 内）
- 导出 ZIP 的图片文件名取自你选择的原始文件名；同名文件会被后一张覆盖
- 表格单元格内**不支持单元格级链接**（如需整行跳转可后续加）
- 轮播的自动播放定时器在 Vue 导出中不会在卸载时清除（单页场景无影响）
