# 更新日志

本项目遵循语义化版本，日期格式为 `YYYY-MM-DD`。
更早的历史提交请查看 Git 记录。

## [1.1.6] - 2026-09-25

本轮为收尾工程：自动化测试入库、SEO 配置、数据安全（结构可撤销 + 版本快照）、
清理已知小缺口、多页 Vue 导出、组件与模板扩展。

### 🧪 自动化测试入库（334 项断言）

- 新增 `tests/` 目录与 `npm test` / `npm run test:ai` 脚本
- `tests/loader.mjs` 让 Node 直接运行 `src/` 下的浏览器代码：
  补全无扩展名的相对导入、把 `file-saver` 替换为可捕获 ZIP 的桩、注入 localStorage 等最小环境
- 五个测试套件：AI 链路（链接/契约/Prompt/解析）、编辑器 store（画布/页面/草稿/操作）、
  导出管线（组件/隐藏/ZIP/多页整站/SEO/Vue）、组件库与 Schema 一致性、
  真实 AI 接口（可选，`VWE_KEY_FILE` 指定 Key 文件）
- 纯逻辑测试 0.1 秒完成；真实 AI 测试约 2 分钟（连接 + 单页生成 + 多页生成 + 应用 + 导出）

### 🔍 SEO 配置

- 页面新增 SEO 字段：`seoTitle`（浏览器标题）、`seoDescription`、`seoKeywords`、
  `seoFavicon`、`seoOgImage`（社交分享配图）、`seoLang`（`<html lang>`）
- 属性面板「📄 页面」新增「SEO 与页面信息」编辑区
- 导出 HTML 时注入 `<title>` / meta description / keywords / favicon / og 标签；
  留空的项不会输出，标题留空时回退为页面名
- SEO 字段贯通全链路：页面状态 → 草稿 → Schema 清洗 → 页面契约（AI 可填写）→ 导出
- AI Prompt 要求为每个页面填写 SEO 字段（单页与多页模式均包含）

### 🐛 修复：escapeAttr 未转义尖括号

- `escapeAttr` 原本只转义 `&` `"` `'`，未处理 `<` `>`；
  虽然双引号属性内不转义通常不会逃出属性，但在单引号属性或后续拼接场景下会造成标签注入，
  与该函数注释声明的「防 XSS 与语法错误」不符
- 由新增的 SEO 转义测试发现，现已补上 `<` `>` 转义

### 📈 新增 4 个展示组件 + 数据看板预设

- **进度条 (progress)**：当前值/最大值、条色与轨道色、条高、圆角、百分比文字
- **折叠面板 (accordion)**：每行 `标题|内容`，默认展开首项、是否允许多开；
  导出使用**原生 `<details>`**（无 JS 即可展开），多开关闭时追加一小段互斥脚本
- **标签徽章 (badge)**：文字、背景色、胶囊/圆角方形、可选描边、字重
- **统计卡片 (stat)**：主数值 + 单位 + 说明文字 + 趋势（升降决定箭头与颜色）+ 可选图标
- 新增「展示组件」分类；四个组件都打通：默认配置 → 画布预览 → 属性面板 → HTML 导出 → Vue 导出 → AI Schema
- 新增预设模板 **「数据看板页」**：四张统计卡片 + 三条进度条 + 渠道明细表格 +
  折叠面板 FAQ + 徽章 + 分隔线 + 深色页脚，覆盖全部新组件

### ♻️ 预设模板抽成独立模块

- 预设数据从 `ComponentList.vue` 抽到 `utils/presetPages.js`，便于测试与扩展
- 新增预设校验测试，自动检查：结构完整、**组件类型已知**、**组件不越界**、**props 键在白名单内**
- 该校验立刻发现并修复了一个既有问题：「博客文章页」页面高度 1950 装不下底部容器（1960）

### 🧩 整站 Vue 工程导出

- 新增 `generateVueSiteFiles(canvas)` / `exportVueSiteWithImages(canvas)`：
  把当前画布的所有页面导出为完整的 **Vue 3 + Vite + Vue Router** 工程
  - 生成 `package.json` / `vite.config.js` / `index.html` / `README.md`
    / `src/main.js` / `src/App.vue` / `src/router/index.js` / `src/assets/base.css`
  - 每个页面生成 `src/views/<ViewName>.vue`（视图名由英文 slug 转 PascalCase）
  - 路由使用 **hash 模式**（`createWebHashHistory`）：静态部署或直接打开文件都能跳转
  - 页面之间的 `#page:<id>` 链接被重写为路由路径（`#/about-us`）
  - 路由 `meta.title` 取自页面 SEO 标题，切换路由时自动更新 `document.title`
  - 含本地图片时一并打包 `images/` 并在 README 中提示
- 工具栏「导出 ▼」菜单新增「导出整站 Vue 工程」
- 顺带把 `exportVueWithImages` 里重复的本地图片收集逻辑统一到
  `collectLocalImagesFromPages`（修掉它遗留的文件名重复扩展名问题）

### 🔒 数据安全：结构变更可撤销 + 历史版本

- **结构性操作纳入撤销栈**：新建/删除/重命名/复制/排序画布与页面、
  以及「应用整站」现在都能 `Ctrl+Z` 退回（此前只有组件级操作可撤销，误删页面无法恢复）
  - 历史条目分两种范围：组件级只快照当前页面，结构性操作快照整个项目
  - 页面级快照记录页面 id，撤销跨页面编辑时会**自动切回目标页面**
- **历史版本快照**（新增 `utils/versionStore.js` + `VersionPanel.vue`）：
  - 工具栏「📚 版本」可把整个项目保存为具名快照，支持恢复 / 重命名 / 删除 / 清空
  - 列表显示保存时间、规模（画布/页面/组件数）与体积；最多保留 20 个
  - 单个快照超过 2MB 会拒绝保存并提示（本地图片以 data URL 存储时容易变大）
  - 恢复操作本身也进撤销栈，可用 `Ctrl+Z` 退回

### 🔧 清理已知小缺口

- **navMenu 的 `alignment` 生效**：此前该属性只存在于默认值与 Schema，渲染与导出都没用它；
  现在编辑器预览、HTML 导出、Vue 导出都按 left/center/right 分配菜单主轴，属性面板也补上了该选项
- **视频封面支持本地图片**：属性面板新增「📁 选择本地封面图」，
  封面以 data URL 存草稿，导出时打包进 ZIP 的 `images/` 并重写路径（`hasLocalImages` 同步支持）
- **表格单元格支持链接**：单元格可写 `[文字](链接)`（链接文字可读，支持 `#page:页面名`），
  或直接填 `https://` 网址自动识别为链接；新增 `linkTarget` 控制打开方式（当前窗口/新窗口）

## [1.1.5] - 2026-09-25

本次更新让 AI **感知站点页面结构**，并能**一次生成整个站点（多页面）**。

### 🤖 AI 页面感知与多页生成

- **页面感知**：单页生成的 Prompt 现在会带上「本站页面清单」（页面名 / 是否当前页 / 组件数），
  AI 写导航栏、页脚、按钮时会用 `#page:<页面名>` 指向真实页面
- **多页生成**：新增「生成整个站点（多个页面）」范围。AI 一次输出
  `{ siteName, pages: [...] }`，每页独立执行 Schema 校验与重叠修复
- **自动建页与链接串联**：应用时在当前画布创建这些页面（默认**追加**，可选替换），
  并把以页面名书写的跨页链接解析为真实页面 ID；结果提示会显示串联了多少个链接
- **英文 slug**：AI 为每页给出英文文件名（`products` / `about` …），
  导出整站时用作文件名，中文页面名不再退化成 `page-2.html`
- 新增 `utils/pageLinks.js`：页面链接的构建 / 解析 / 统计；名称→ID 的解析对
  `href`、导航菜单项、面包屑、列表项、轮播图等**所有字符串属性统一生效**
- 多页页面名自动去重（同名加序号，避免跳转链接歧义）；单次最多接受 12 个页面
- AI 面板新增「生成范围」选择、「期望页面数」、「替换当前画布」选项与逐页结果清单

### 🐛 修复：AI 生成的坐标被当前页尺寸裁剪

- 单页生成时原本把组件坐标按**当前页面尺寸**（如 1200×800）裁剪，
  导致 AI 写出的高页面被压扁进 800px 内，产生大量本可避免的重叠
- **实测对比**：修复前一次生成 41 个组件产生 **214 处重叠**（全靠修复逻辑摊开）；
  修复后一次生成 4 页 174 个组件 **0 处重叠**
- 现在使用 Schema 默认边界（3840 × 8000），保留 AI 原本推算好的坐标

### 🐛 修复：顶层数组形式的 AI 回复无法解析

- `extractJSONObject` 原本只按 `{ ... }` 截取，若模型直接返回数组 `[ ... ]`，
  方括号会被切掉导致解析失败；现在按更早出现的 `{` 或 `[` 取配对内容

### 📄 文档

- README 新增「AI 页面感知与多页生成」章节

## [1.1.4] - 2026-09-24

本次更新聚焦**多画布 / 多页面**能力，并修复一个本地图片导出问题。

### 🗂 多画布 / 多页面（项目 → 画布 → 页面 → 组件）

- **多画布**：一个画布 = 一个完全独立的设计空间，画布之间的页面与组件互不影响；
  支持新建 / 重命名 / 删除画布（至少保留一个）
- **多页面**：每个画布可包含任意多个页面，支持新建 / 重命名（双击标签）/ 删除 / 复制（含全部组件，
  组件 id 自动重新生成）；工具栏下方新增「画布 + 页面」切换栏，实时显示页面数与组件总数
- **页面间跳转**：属性面板新增「🔗 页面跳转」选择器（文字 / 图片 / 按钮），
  并为导航菜单项、面包屑、列表项、轮播图逐项提供「跳转到页面…」下拉；
  内部记录为 `#page:<页面ID>`，导出时自动重写为对应页面文件名
- **导出整站**：新增 `exportSiteWithImages`，把当前画布所有页面打包为 ZIP
  （首个页面 `index.html`，其余按名称生成 slug，中文名回退 `page-N.html`，重名自动加序号），
  页面间链接互相串联，含本地图片时一并打包 `images/`
- **草稿升级为项目级**（version 2.0）：保存所有画布与页面、当前停留位置；
  **旧版单页草稿自动迁移**为「一个画布 + 一个页面」
- 结构调整：`page` 改为响应式导出（切换页面后各面板自动指向新页面），
  页面重置保留页面 id（避免破坏页面间跳转链接），自动保存改为监听整个项目

### 🐛 修复：本地图片导出文件名重复扩展名

- 导出 ZIP 时原始文件名已含扩展名，又被追加一次，出现 `photo.png.png`
- 现统一去掉原扩展名，扩展名以图片数据真实类型为准（新增 `buildImageFileName`）

### 🔧 AI 调用健壮性（用真实 Key 实测后调整）

- **实测数据**（OpenCode Go + `deepseek-v4.1-flash`）：极短请求约 2 秒、
  完整页面 JSON 约 40 秒、成品网页长 HTML 约 60 秒
- 默认超时 **120 秒 → 180 秒**：JSON 模式失败后的回退重试原本与首次尝试**共用同一份超时预算**，
  首次尝试偏慢就会把重试的预算耗光，导致整体报「请求超时」
- 改为**每次尝试独立计时**（各自独立的 `AbortController` 与计时器，见 `createAttempt`），
  重试拥有完整超时预算；外部 `signal` 取消仍然生效
- 超时提示补充可操作建议（稍后重试或换更快的模型）

## [1.1.3] - 2026-09-23

本次更新包含三部分：**图层面板选择语义完善**、**新增 6 个内容组件**、**内容组件增强（逐项编辑 / 跳转 / 本地图片打包）**。

### ✨ 图层面板：单击 / 双击选择语义

- **单击**图层行 = 只选中该组件
- **双击**图层行 = 连同「它内部包含的所有组件」一起选中，与在画布上单击容器的行为完全一致，
  便于整体移动 / 对齐 / 分布 / 微调
  - 只有**完全落在**该组件矩形内的组件才会被一起选中（部分重叠不算）
  - 锁定的子组件不会被纳入选择集
- 重命名入口由「双击名称」改为行内 **✎ 按钮**（避免与双击组选冲突，也更易发现）
- 「按位置」模式下隐藏层级值与拖拽手柄，给图层名称留出更多宽度

### 🔒 修复：锁定组件的连带移动漏洞

- 画布上拖动容器时，原本会把容器内**已锁定**的子组件一起拖走（锁定保护不完整）
- 现在 `getChildrenInsideComponent` 与 `moveComponents` 都会跳过锁定组件，
  组选中同样不会把锁定子组件纳入选择集，锁定语义在全链路上保持一致

### 🛡 健壮性：避免热更新不同步导致画布不可用

- 画布选中逻辑改为经 `selectWithChildren` 调用 store 的组合动作，并做存在性回退
  （回退到 `selectComponent` / `getChildrenInsideComponent` 基础 API）
- 背景：开发时若组件实例已更新而 store 模块未同步，直接调用新方法会在 `mousedown`
  的选中环节抛异常，导致「选中」与「拖拽」同时失效；加回退后可保证功能始终可用
- README 开发说明补充该现象的排查方法（硬刷新 / 重启 dev 服务器）

### ✨ 新增：6 个内容组件（组件库 15 → 21 种）

| 组件 | 能力 |
| --- | --- |
| 分隔线 (divider) | 实线 / 虚线 / 点线，线宽与颜色可调，可带中间文字 |
| 图标 (icon) | emoji / 字符，可选圆形或圆角方形背景 |
| 列表 (list) | 无序 / 有序 / 无标记，自定义标记符号与颜色、项间距，每项可设跳转链接 |
| 表格 (table) | 表头 + 数据行，表头配色、边框色、斑马纹、单元格内边距可调 |
| 视频 (video) | 视频直链（mp4，含封面 / 控制条 / 自动播放 / 静音 / 循环）或 iframe 嵌入 |
| 轮播图 (carousel) | 每张图独立配置（图片 / 说明 / 跳转链接），自动或手动切换、指示点、左右箭头 |

- 每个组件均打通完整链路：**默认配置 → 画布预览组件 → 属性面板编辑 → HTML 导出 → Vue 导出 → AI Schema 契约**
- 轮播图导出时带**自包含交互脚本**（箭头切换、指示点跳转、可选自动播放），HTML 与 Vue 导出均可交互
- 视频自动播放默认静音（浏览器策略要求）；编辑态轮播不自动播放，避免干扰编辑
- 左侧组件栏新增「内容组件」分类；图层面板与组件列表补充对应图标
- 属性面板新增 `number` 与 `color` 两种字段类型（此前仅支持 select / textarea / text / checkbox）

### ✨ 逐项编辑（每张图 / 每行文字独立成组）

- **轮播图**：属性面板改为「🖼 图片与跳转配置」，每张图独立成一块，含
  缩略图预览、本地图片按钮、图片地址、说明文字、跳转链接；支持「+ 添加图片」与逐张删除
- **列表**：改为「📋 列表项配置」，每项独立成一块（文字 + 跳转链接），支持增删
- **表格**：改为「📊 表格内容配置」，**表头按列**、**数据行按行**逐项编辑，
  支持加列/删列、加行/删行、单元格级编辑（表头与数据行的列数自动保持一致）
- 编辑结果仍写回原有的 `|` / 换行分隔字符串格式，**完全兼容旧数据与 AI 生成的数据**

### 🔗 图片与文字支持超链接跳转

- 轮播每张图可单独设置跳转链接；列表每一项可单独设置跳转链接
- 新增「链接打开方式」（当前窗口 / 新窗口），导出为 `target="_self" / "_blank"`
- 导出时带链接的图片输出 `<a>` 包裹（轮播的切换脚本改为作用于 `.carousel-slide`，两种结构都能正确切换）
- **编辑态点击链接不会跳走**（`preventDefault`），只有预览态与导出文件才真正跳转

### 🖼 本地图片与导出打包

- 轮播每张图支持「📁 本地图片」，可在同一组件内为不同张选择不同本地图片
- 本地图片以 data URL 存入页面数据 → **草稿刷新不丢**；
  导出时自动收集进 ZIP 的 `images/` 目录并重写路径（HTML 与 Vue 两条导出链路都已支持）
- 本地图片的文件名记录在轮播行内的第 4 个字段（`地址|说明|链接|文件名`），增删/重排不会导致文件名错位
- `hasLocalImages` 增加轮播检测，含本地图片时自动走 ZIP 导出

### 🔧 导出管线

- `getComponentTag` / `generateVueComponentTemplate` 新增 6 个组件的生成分支
- `generateVueScript` 新增轮播的 `carouselShow` / `carouselStep` 方法（沿用既有 tabs 的 DOM 操作约定）
  与自动播放定时器；生命周期块经回归验证仍只生成一次 `onMounted` / `onUnmounted`
- 轮播与列表导出时支持超链接包裹，并支持本地图片路径替换

### 📄 文档

- README 组件列表补充「内容组件」章节与逐项编辑、跳转、本地图片说明，Roadmap 同步

## [1.1.2] - 2026-09-23

本次更新新增「图层面板」，用于管理复杂页面的组件层级。

### ✨ 新增：图层面板（组件层级管理）

- 左侧边栏新增「🧩 图层」标签页（`src/components/panels/LayerPanel.vue`）
- **自动推断父子层级并折叠成树**：新增 `src/utils/layerTree.js`，
  通过几何包含关系判定「组件是否落在某个容器内」，自动形成父容器 → 子组件的树形结构
  - 只有 `container` 类型可作为父容器；包含判定带 2px 容差
  - 同一组件落在多个容器内时，优先归入「书写顺序更晚声明」的容器
    （例如整条通栏横幅里的页脚元素不会被误判成某张卡片的子元素）
  - 支持多层嵌套（容器套容器）；对完全重合的容器做了防环处理
  - 父子关系完全由位置决定：把组件移出容器范围即自动脱离分组，无需手动操作
  - 同一套判定规则被布局修复（`layoutRepair.js`）复用，避免两处逻辑产生分歧
- 图层列表提供两种排列方式（面板顶部切换）：
  - **按位置（默认）**：按页面位置从上往下排列，同一水平位置时大块在前（容器 → 其子元素缩进跟随），
    即"页面大纲"式阅读顺序，与画布上的视觉顺序一致
  - **按层级**：按叠放顺序排列（最上层在前），此模式下支持拖拽调整同级叠放顺序
- 每行显示折叠开关、类型图标、名称、子组件数量、层级值与操作按钮
- 点击选中，`Ctrl` / `Cmd` 点击多选；鼠标悬停图层行会在画布上虚线高亮对应组件
- **同级拖拽调整叠放顺序**（仅「按层级」模式；重排只影响同级之间的相对顺序，与非同级组件的叠放关系保持不变）；
  也可用「置顶 / 上移 / 下移 / 置底」按钮
- 重排后自动归一化 `zIndex` 为连续值，保证「面板顺序 = 最终叠放顺序」
- **全部展开 / 全部折叠** 快捷按钮；**双击名称重命名**（与属性面板中的组件名同步）
- **显示 / 隐藏**：隐藏的组件不渲染、不参与导出（HTML 与 Vue 导出都会跳过，也不会残留孤儿样式）
- **锁定 / 解锁**：锁定后画布上不可拖动、不可双击编辑、不显示缩放手柄；
  也不会被批量操作影响（微调、对齐、分布、删除、全选、框选都会跳过锁定组件），
  只能通过图层面板解锁；锁定组件选中时边框显示为橙色虚线以示区分

### 🔧 其他调整

- 组件数据模型新增 `visible` / `locked` 字段（`createComponent`、导入规范化、Schema 契约、AI 契约同步）
- `zIndex` 上限由 5 放宽到 999（图层面板重排需要连续层级值），AI Prompt 中的层级说明同步更新
- 侧边栏标签栏改为紧凑图标样式，容纳 4 个标签（组件 / 图层 / 页面 / 布局）

### 📄 文档

- README：功能特性与 Roadmap 同步（图层面板移入已完成）

## [1.1.1] - 2026-09-23

本次更新聚焦「编辑效率」与「预览/导出一致性」。

### ✨ 新增：多选对齐与等距分布

- 属性面板新增「🧩 多选操作」区（`src/components/panels/PropertyPanel.vue`）
- 对齐：左 / 右 / 水平居中、顶 / 底 / 垂直居中（需选中 ≥ 2 个）
- 等距分布：水平 / 垂直（需选中 ≥ 3 个，保持首尾组件不动，中间等间距重排）
- 均为**原子操作**，一次 `Ctrl+Z` 即可整体回退；只影响选中组件，未选中的不动

### ✨ 新增：复制粘贴与快捷键体系

- `Ctrl/Cmd+C` 复制、`Ctrl/Cmd+V` 粘贴（+20px 偏移、生成新 id、自动选中新组件）
- `Ctrl/Cmd+D` 原地再制、`Ctrl/Cmd+A` 全选
- `Ctrl/Cmd+S` 立即保存草稿（同时拦截浏览器"保存网页"）
- 方向键微调位置 1px，`Shift` + 方向键 10px（连续微调只在首次记录撤销点，不刷爆撤销栈）
- `Delete` / `Backspace` 支持**多选删除**，一次撤销可全部恢复
- 在输入框 / 文本域等编辑控件内不劫持快捷键

### 🔧 修复：预览与导出一致性

- **文本 / 按钮 / 链接的对齐在导出时丢失**：属性面板把对齐写入 `props.textAlign`，
  而导出只读 `style.textAlign`，导致导出后对齐设置不生效。现改为 `style` 优先、`props` 回退
- **移除失效的百分比定位管线**：`componentStyleToString()` 每次导出都会被计算，但结果被调用方丢弃，
  导出实际一直是固定像素布局（已删除该函数及 18 处无人使用的 `style` 字段）
- 导出定位方式现在明确为「固定像素绝对定位」，README 不再宣称响应式导出

### 📄 文档

- README Roadmap 按实际代码状态重写，区分已完成 / 进行中 / 规划中，并标注已知小问题
  （`navMenu.alignment` 在预览与导出中均未实现）

## [1.1.0] - 2026-09-23

本次更新围绕「优化编辑器 + 新增 AI 辅助建站」两大目标，共修改/新增 20 余个源文件。

### ✨ 新增：AI 辅助建站

- 工具栏新增「🤖 AI 生成」入口与生成面板（`src/components/panels/AiPanel.vue`）
- **服务商**：DeepSeek 官方（默认）、OpenCode Go（均为 OpenAI 兼容接口）
- **默认模型**：DeepSeek V4.1（官方 API 模型 ID `deepseek-flash`）；切换服务商时 API 地址与模型自动切换
- **API Key 安全策略**：通过**本地文本文件**加载（支持 `sk-xxx`、`KEY=sk-xxx`、`#` 注释行等格式），
  Key 只存在于会话内存，**不写入 localStorage、不进入页面代码**；旧版本残留在浏览器里的 Key 会在读取时自动清除
- **生成模式**：补全当前页面（保留已有布局）/ 整体重写 / 追加新区块
- **四条产出链路**：
  1. 回到画布继续编辑（绝对坐标，应用后可 Ctrl+Z 撤销）
  2. **直接生成成品网页**：AI 以画布为需求草图，输出语义化标签 + flex/grid 文档流 + 响应式的完整 HTML（不含绝对定位），一键下载 `index.html`
  3. 按画布像素布局导出 HTML
  4. 按画布像素布局导出 Vue 工程
- 生成前提供差异预览（保留 / 新增 / 移除组件统计）与成品代码预览、复制

### 🛡️ 修复：AI 生成页面组件堆叠

- **现象**：AI 把「绝对定位画布」误当成文档流，把大量组件写在同一个 `top` 上，页面重叠成一团
- **Prompt 层**：写入硬性坐标规则 —— 禁止复用同一 `top`、上下相邻组件必须累加高度、
  附完整坐标推算示例、限定 `zIndex` 为 1~5、明确 `textAlign` 等字段归属
- **代码层**：新增布局自动修复 `src/utils/layoutRepair.js`
  - 容器内子元素真正相交时逐行排开，并自动撑高容器
  - 顶层区块相交时按 AI 书写顺序整体下移（保持阅读顺序）
  - 保留本来正确的并排行（同一行 left 不相交的元素不被拆散，同高度并排行整组同进同退）
  - 按内容自动补足页面高度；修复是幂等的，正常页面零改动
  - 面板提示「已自动修复布局重叠（N 处 → 0 处）」

### 🌐 修复：浏览器跨域（CORS）

- **现象**：第三方 AI 接口不返回 `Access-Control-Allow-Origin`，浏览器直连必然 `Failed to fetch`
- **方案**：新增 Vite 本地代理（`/api-proxy/deepseek`、`/api-proxy/opencode-go`），
  前端在本地运行时自动改用同源代理路径，由开发/预览服务器转发到真实端点
- 代理转发时携带自定义 UA 与会话头；错误分类细化（网络 / 鉴权 / 限流 / 服务端 / 超时）

### 🔧 修复：预设模板与渲染一致性

- 修复 19 个预设按钮误用 `props.text`（导致导出后按钮文字全变成"按钮"）
- 修复渐变容器的字段写法，统一为 `backgroundType` + `backgroundGradient*` 规范
- 新增单位感知处理：`50%`、`24px`、`4px 12px` 等值不再被错误拼接成 `50%px` / `24pxpx`

### ♻️ 优化：编辑体验

- **撤销/重做补全**：快照包含完整页面与背景字段；预设页面应用改为原子操作（一次撤销整体回退）；
  导入、页面设置、层级调整等操作全部纳入历史
- **本地草稿自动保存**：防抖写入 localStorage，启动自动恢复，状态栏显示保存时间与恢复提示
- **导入校验加强**：未知组件类型自动过滤，缺失字段与默认值合并，避免渲染缺字段
- **组件 Schema 数据契约**：新增 `src/utils/componentSchema.js`，作为导入校验与 AI 提示词的唯一依据
- **一键启动脚本**：新增 `启动编辑器.bat`（检测 Node、首次自动安装依赖、启动并自动打开浏览器）；
  脚本以 GBK + CRLF 保存（中文 Windows 的 cmd 解析要求）

### 📄 文档

- README 补充：AI 使用步骤、两条产出链路对照表、组件重叠自动修复说明、CORS 与 Key 安全说明
- 新增 `.gitattributes`：统一换行符，并强制 `.bat` 保持 CRLF
- 修正 `.gitignore`：移除「忽略自身」的错误配置，新增本地 API Key 文件忽略规则

---

## [1.0.0] - 初始版本

- 拖拽式可视化编辑、吸附辅助线、缩放与预览
- 基础组件（文本/图片/按钮/容器/链接/日期时间）、预设表单（登录/注册/联系/搜索/评论/自定义）、导航组件（导航菜单/面包屑/标签页）
- 预设页面模板与常用布局
- JSON 页面/布局导入导出
- HTML 导出（含本地图片打包）、Vue 工程导出
- 后端代码生成（Spring Boot / Express / Flask）
- 撤销/重做、快捷键
