# 统一换行符：仓库内文本文件使用 LF
* text=auto eol=lf

# Windows 批处理必须保持 CRLF，否则 cmd 解析会出错
# （启动编辑器.bat 同时是 GBK 编码，用于匹配中文 Windows 的代码页 936）
*.bat text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.woff binary
*.woff2 binary
