# 基于 Vue3 + Vite 打造的轻量级可视化网页编辑器，支持组件拖拽、实时预览与 HTML 导出。
##  项目状态
早期 MVP 原型，整体开发进度约 40%，核心编辑功能可用（拖拽、吸附、撤销/重做、JSON/HTML/Vue 导出、预设模板、后端代码生成、本地草稿自动保存）。"AI 辅助建站"功能正在开发中。代码未做全面优化、边界校验及生产环境适配，请勿直接用于线上生产项目。
## 功能特性
- 拖拽式编辑：通过拖拽组件到画布快速搭建页面
- 丰富的组件库：内置文本、图片、按钮、表单、导航等常用组件
- 多选对齐与分布：左/右/居中、顶/底/垂直居中、水平/垂直等距（一次撤销可回退）
- 图层面板：拖拽排序、显示/隐藏、锁定、重命名，hover 与画布联动
- 复制粘贴与快捷键：`Ctrl+C/V/D/A`、方向键微调、`Ctrl+S` 保存、多选删除
- 实时预览：所见即所得，编辑效果即时展示
- 一键导出：打包生成可独立运行的纯 HTML 文件，另支持 Vue 单文件/工程导出
- 预设模板：内置多套页面模板与常用布局，快速起步
- 本地图片支持：上传本地图片并随同页面一起导出
- 草稿自动保存：编辑内容防抖自动保存到浏览器本地，刷新不丢失
- 数据契约：组件 Schema 统一校验（导入时自动过滤未知类型）
- AI 辅助建站：在画布里摆放好组件后，点击工具栏"🤖 AI 生成"，由 AI 补全为完整网页（可回画布继续编辑，或直接导出完整 HTML / Vue）
## 技术栈
- 前端框架：Vue 3 (Composition API)
- 构建工具：Vite
- 状态管理：Vue3 Reactive
- 样式方案：CSS3 + Scoped Styles
- 文件导出：JSZip + FileSaver
## 安装和运行

# 安装项目依赖
npm install

# 启动本地开发服务（浏览器自动打开）
npm run dev -- --open

# 或直接双击项目根目录下的 启动编辑器.bat（一键：检测 Node、首次自动装依赖、启动并打开浏览器）

# 打包构建生产版本
npm run build

# 本地预览打包产物
npm run preview

> **开发小贴士**：修改 `src/stores/*.js` 这类普通 JS 模块后，Vite 热更新偶尔会出现
> 「组件已更新但 store 模块未同步」的情况，表现为点击无响应 / 组件拖不动。
> 遇到这类异常先**硬刷新页面（Ctrl + Shift + R）**，仍不行则重启 dev 服务器。

快速开始
0. （推荐）双击项目根目录的 `启动编辑器.bat`，脚本会自动检测 Node、首次运行自动安装依赖、启动开发服务器并打开浏览器
1. 启动项目后，从左侧组件栏选择组件
2. 拖拽组件至中间画布完成布局
3. 在右侧属性面板修改组件内容、样式
4. 点击预览按钮查看最终效果
5. 点击导出，生成并下载完整 HTML 文件

AI 辅助建站（Beta）
1. 在画布中先摆放几个组件作为"骨架"（或直接应用左侧的预设页面）
2. 点击工具栏"🤖 AI 生成"打开面板
3. 在面板中选择服务商（支持 **DeepSeek 官方** 与 **OpenCode Go** 两家，均为 OpenAI 兼容接口），点击"📄 选择 Key 文件"从本地文本文件加载 API Key，可先点"测试连接"
4. 输入页面类型与你的要求，选择风格、生成模式（补全/重写/追加）与输出方式
5. 点击"开始 AI 生成"，在结果预览中确认后点"应用"

### 两条产出链路（重要）
编辑器画布是**绝对坐标**模型（每个组件记录 left/top 像素），它只适合在编辑器里所见即所得地摆放；
真正交付的网页不应该由绝对坐标翻译而来。因此 AI 面板提供两类输出：

| 输出方式 | 说明 | 适用场景 |
| --- | --- | --- |
| **回到画布继续编辑** | AI 在画布坐标系内补全页面，应用后可继续拖拽微调（可 Ctrl+Z 撤销） | 还想继续手工调整 |
| **直接生成成品网页（AI 写 HTML/CSS）** | AI 把画布当成**需求草图**，重新输出一份语义化标签 + flex/grid 文档流 + 响应式的完整 HTML（不含绝对定位），直接下载 `index.html` | 要交付/部署的正式页面（推荐） |
| 按画布像素布局导出 HTML / Vue | 把画布上的绝对像素位置原样翻译成 HTML / Vue 工程 | 需要 1:1 还原你摆放的效果 |

### 多画布 / 多页面

编辑器采用 **项目 → 画布 → 页面 → 组件** 的四层结构：

- **画布（Canvas）**：一个完全独立的设计空间（可理解为一个站点）。不同画布之间的页面与组件互不影响，
  适合同时做多套互不相干的页面（如"官网"和"活动页"）
- **页面（Page）**：画布内的一个网页。工具栏下方有画布选择器与页面标签页：
  - 单击标签切换页面，**双击标签重命名**，`×` 删除页面（每个画布至少保留一页）
  - `＋ 页面` 新建、`⧉ 复制` 复制当前页面（含全部组件，组件 id 自动重新生成）
- **页面间跳转**：在属性面板的「🔗 页面跳转」里选择目标页面即可
  （文字/图片/按钮，以及导航菜单项、面包屑、列表项、轮播图的链接都支持）。
  编辑器内部记录为 `#page:<页面ID>`，**导出整站时会自动重写为对应页面的文件名**

**导出整站**：工具栏「导出整站」把当前画布的**所有页面**打包成一个 ZIP
（第一个页面为 `index.html`，其余按页面名生成如 `about-us.html`，中文页面名回退为 `page-N.html`），
页面之间的跳转链接会互相串联，解压即可直接部署。含本地图片时 `images/` 目录一并打包。

**导出整站 Vue 工程**（工具栏「导出 ▼」菜单）：把当前画布导出为完整的
**Vue 3 + Vite + Vue Router** 工程，每个页面一个视图组件：

```
package.json  vite.config.js  index.html  README.md
src/main.js  src/App.vue  src/assets/base.css
src/router/index.js        # 每个页面一条路由，meta.title 取自页面 SEO
src/views/Home.vue …       # 视图名由英文 slug 转 PascalCase
images/…                   # 本地图片（如有）
```

- 使用 **hash 路由**，`npm run build` 后可直接静态部署（无需服务端重写规则）
- 页面之间的跳转已生成为路由路径；路由切换时自动更新 `document.title`

> 草稿会保存整个项目（所有画布与页面）以及当前停留的画布/页面；旧版单页草稿会自动迁移。

### AI 页面感知与多页生成

AI 生成现在**知道本站有哪些页面**，并且可以**一次生成整个站点**：

| 生成范围 | 说明 |
| --- | --- |
| **只生成当前页面** | 只处理当前打开的页面，但 Prompt 里会带上「本站页面清单」，因此 AI 写的导航栏 / 页脚会自动指向真实页面（内部记为 `#page:<页面名>`，应用后解析为页面 ID） |
| **生成整个站点（多个页面）** | 一次让 AI 规划并生成多个页面（输出 `{ siteName, pages: [...] }`），应用后自动在当前画布创建这些页面，并把页面之间的跳转链接互相关联 |

- 可设「期望页面数」（1~12）与「替换当前画布已有页面」——默认**追加**，不删除已有内容
- AI 会为每个页面给出**英文 slug**（如 `products`），导出整站时用作文件名：
  `index.html`、`products.html`、`about.html`……（中文页面名不再退化成 `page-2.html`）
- 每一页都会**独立执行 Schema 校验与重叠自动修复**
- 应用后提示会显示：创建了几个页面、串联了多少个页面跳转链接
- 页面名重复时自动加序号（避免跳转链接产生歧义）

### 数据安全：撤销范围与历史版本

**撤销（Ctrl+Z）覆盖两类操作**：

| 范围 | 覆盖的操作 |
| --- | --- |
| 组件级（只快照当前页面） | 增删改组件、拖拽/缩放、样式与属性、对齐分布、复制粘贴、微调、显隐与锁定、页面尺寸/背景/SEO |
| 结构性（快照整个项目） | 新建 / 删除 / 重命名 / 复制 / 排序**画布与页面**，以及「应用整站」 |

> 撤销跨页面的编辑时，编辑器会**自动切回被编辑的那个页面**，不会把内容写到别的页面上。

**历史版本快照**（工具栏「📚 版本」）：把整个项目保存为具名快照，随时回退。

- 每个快照记录：名称、保存时间、规模（画布 / 页面 / 组件数）、体积
- 支持恢复 / 重命名 / 删除 / 清空；最多保留 20 个
- **恢复操作本身也可 `Ctrl+Z` 退回**
- 存在浏览器本地（localStorage）；单个快照超过 2MB 会拒绝保存并提示
  （本地图片以 data URL 存在页面数据里，容易让项目变大）

### SEO 与页面信息

每个页面都有独立的 SEO 配置（属性面板「📄 页面」→「🔍 SEO 与页面信息」）：

| 字段 | 导出结果 |
| --- | --- |
| 浏览器标题 | `<title>`（留空时回退为页面名） |
| 页面描述 | `<meta name="description">` + `og:description` |
| 关键词 | `<meta name="keywords">` |
| favicon 地址 | `<link rel="icon">` |
| 分享配图 | `<meta property="og:image">` |
| 页面语言 | `<html lang="...">` |

- 留空的项**不会输出**对应标签；同时始终输出 `og:type` / `og:title`
- 所有 SEO 内容都会做 HTML 转义，避免标签注入
- **AI 会为每个页面自动填写 SEO 字段**（单页与多页生成模式都包含），生成后可在面板里微调
- 导出整站时每个页面各自带自己的 SEO 信息

### 组件重叠会自动修复
AI 常把"绝对定位画布"误当成文档流，把多个组件写在同一个 `top` 上，导致页面上糊成一团。
系统在 AI 结果进入画布前会做一次几何修复（`src/utils/layoutRepair.js`）：

- 容器内子元素互相重叠时逐行排开，并自动撑高容器
- 顶层区块互相重叠时按阅读顺序整体下移
- 保持本来正确的并排（同一行 left 不相交的元素）不被拆散
- 按内容自动补足页面高度

修复后会在面板上提示"已自动修复布局重叠（N 处 → 0 处）"。同时 Prompt 里也写入了硬性坐标规则，
从源头减少 AI 复用同一 `top` 的情况。

注意：
- API Key 通过本地文本文件加载（或手动输入），**只保存在浏览器内存中（本次会话有效），不会写入 localStorage / 页面代码**；刷新页面后需重新加载。Key 文件格式：一行 `sk-xxx`，或 `KEY=sk-xxx`，可含 `#` 注释与空行
- 默认服务商为 DeepSeek 官方，默认模型为 DeepSeek V4.1（官方 API 模型 ID：`deepseek-flash`）；切换服务商时 API 地址与模型会自动切换
- **DeepSeek 官方**：前往 https://platform.deepseek.com 获取 API Key
- **OpenCode Go**：每月 $10 的订阅服务，前往 https://opencode.ai/auth 订阅并复制 API Key（模型列表见 https://opencode.ai/docs/zh-cn/go/）
- **跨域（CORS）已自动处理**：第三方 AI 接口不允许浏览器直连，因此本地运行时前端改为请求同源路径 `/api-proxy/...`，由 Vite 开发/预览服务器转发到真实端点（配置见 `vite.config.js`）。请务必用 `npm run dev` / `npm run preview` 或 `启动编辑器.bat` 启动，不要直接双击 `index.html`
- 打包产物部署到线上时 Vite 代理不存在，需自行配置后端代理转发（Key 也建议放后端），否则线上会再次遇到 CORS

## 项目结构

- src/
  - assets/ 静态资源
    - styles/ 全局公共样式
  - components/ 通用 Vue 组件
    - canvas/ 画布核心组件
    - layout/ 页面布局组件
    - panels/ 侧边栏、属性面板
    - widgets/ 可拖拽业务组件
    - FormDisplay.vue 表单弹窗展示组件
  - composables/ 组合式工具函数
  - stores/ 全局状态管理
  - utils/ 通用工具方法
  - App.vue 根组件
  - main.js 项目入口文件
# 组件列表

# 基础组件
- 文本 (Text)
- 图片 (Image)
- 按钮 (Button)
- 链接 (Link)
- 日期时间 (DateTime)
- 纯色背景 (Container)
# 表单组件
- 登录表单 (LoginForm)
- 注册表单 (RegisterForm)
- 联系表单 (ContactForm)
- 搜索表单 (SearchForm)
- 评论表单 (CommentForm)
- 自定义表单 (CustomForm)
## 导航组件
- 导航菜单 (NavMenu)
- 面包屑 (Breadcrumb)
- 标签页 (Tabs)
## 内容组件
- 分隔线 (Divider)：实线/虚线/点线，可带中间文字
- 图标 (Icon)：emoji/字符，可选圆形或圆角方形背景
- 列表 (List)：**逐项编辑**（文字 + 跳转链接），无序 / 有序 / 无标记，自定义标记符号
- 表格 (Table)：**表头按列、数据按行逐项编辑**，斑马纹与配色可调；
  **单元格支持链接**：写 `[文字](链接)`，或直接填 `https://` 网址自动变链接
- 视频 (Video)：视频直链（mp4）或 iframe 嵌入，**封面图可选本地图片**
- 轮播图 (Carousel)：**每张图独立成组**（图片 + 说明文字 + 跳转链接），
  支持**选择本地图片**并随导出一起打包，多图自动/手动切换、指示点与左右箭头（导出时带交互脚本）

## 展示组件
- 进度条 (Progress)：当前值/最大值、条色与轨道色、条高、圆角、百分比文字
- 折叠面板 (Accordion)：每行 `标题|内容`，可设默认展开与是否允许多开
  （导出使用**原生 `<details>`**，无需 JS 即可展开）
- 标签徽章 (Badge)：文字、背景色、胶囊/圆角方形、可选描边
- 统计卡片 (Stat)：主数值 + 单位 + 说明 + 趋势（升降自动切换箭头与颜色）+ 可选图标

> 图片类内容支持「选择本地图片」：图片以 data URL 存在页面数据里（草稿刷新不丢），
> 导出时自动打包进 ZIP 的 `images/` 目录并重写路径，无需图床。
开发计划（Roadmap）

### ✅ 已完成
- 基础组件开发（25 种：基础 6 + 表单 6 + 导航 3 + 内容 6 + 展示 4）
- 预设页面模板 7 套（企业介绍 / 产品展示 / 博客文章 / 联系我们 / 用户登录 / 数据统计 / 数据看板）
- JSON模板导入导出（带类型校验与默认值合并）
- 简单的预设页面和布局
- 拖拽吸附辅助线
- 撤销/重做（覆盖全部页面/组件修改动作，快照含完整背景字段）
- 预设模板修复：按钮字段统一、渐变容器规范化
- 本地草稿自动保存/恢复（防抖 + 自动清理）
- 组件 Schema 数据契约（AI 开发基础）
- HTML / Vue 导出（含本地图片打包）
- AI 辅助建站：服务层（DeepSeek 官方 / OpenCode Go）、Key 文件加载（仅内存不落盘）、
  Prompt 构建、响应容错解析、布局重叠自动修复、差异预览、
  四条产出链路（回画布 / AI 直写成品网页 / 像素导出 HTML / 像素导出 Vue）
- 本地 Vite 代理转发，解决第三方 AI 接口的浏览器跨域（CORS）
- **多选对齐与等距分布**：左/右/水平居中、顶/底/垂直居中、水平/垂直等距（一次撤销可回退）
- **复制 / 粘贴 / 原地再制 / 全选**，方向键微调（1px，Shift 为 10px）
- **快捷键体系**：缩放 `Ctrl±/0`、撤销重做 `Ctrl+Z`/`Shift+Z`、辅助线 `Ctrl+G`、
  删除、`Esc`、`Ctrl+C/V/D/A`、`Ctrl+S` 保存草稿
- **预览与导出一致性修复**：文本/按钮对齐字段来源统一（`props.textAlign` 之前被导出忽略）；
  移除失效的百分比定位管线（导出明确为固定像素布局）
- **图层面板**：**自动按位置推断父子层级并折叠成树**（容器包住的组件自动成为其子层）；
  默认**按页面位置展示（从上往下、大块在前、子元素缩进跟随）**，可切换为「按层级」（最上层在前，支持同级拖拽排序）；
  **单击选中单个组件，双击连同它内部的组件一起选中**（同画布上单击容器），便于整体移动/对齐；
  另有点击多选（`Ctrl`）、置顶/上移/下移/置底、✎ 重命名、显示/隐藏、锁定/解锁、hover 与画布联动高亮；
  隐藏组件不参与导出，锁定组件不参与拖动与批量操作

### 🚧 进行中
- AI 生成质量持续优化（Prompt 迭代、更多风格与页面类型）
- 预设页面和布局优化（模板数量扩展）
- 更多组件库扩展（已新增 6 个内容组件，后续可加：进度条、折叠面板、标签徽章、统计卡片、地图等）
- 响应式预览（编辑器内的断点预览；交付侧响应式已由 AI 成品网页链路覆盖）

### 📋 规划中
- 多页面项目管理
- 历史版本管理界面（基于现有草稿机制做版本快照）
- SEO 配置（导出时注入 `<head>`）
- 组件仓库（自定义组件保存与复用）
- 自动化测试入库（当前测试在开发环境临时运行）
## 开发说明

本项目在开发过程中使用了 AI 工具辅助编写代码、调试与优化文档。
所有核心设计与功能逻辑均由作者独立完成。

### 自动化测试

```bash
# 纯逻辑测试（不联网，秒级完成，334 项断言）
npm test

# 额外包含真实 AI 接口测试（需要 Key 文件，约 2 分钟）
set VWE_KEY_FILE=E:\path\to\key.txt
npm run test:ai
```

测试直接运行 `src/` 下的浏览器代码（`tests/loader.mjs` 负责补全无扩展名的相对导入、
把 `file-saver` 换成可捕获 ZIP 的桩、注入 `localStorage` 等最小环境）：

| 套件 | 覆盖内容 |
| --- | --- |
| `tests/ai.test.mjs` | 页面链接解析、站点契约、单页/多页 Prompt、JSON 容错提取、响应解析与重叠修复 |
| `tests/editor.test.mjs` | 画布/页面 CRUD 与隔离、多页应用与链接串联、草稿持久化与迁移、撤销重做、对齐分布、移动与锁定、图层树 |
| `tests/export.test.mjs` | 各组件导出、隐藏组件过滤、本地图片 ZIP、多页整站 ZIP 与链接重写、SEO head 注入、Vue 导出 |
| `tests/components.test.mjs` | 组件分类/默认配置/Schema 白名单/createComponent 四者一致性、清洗行为、页面契约 |
| `tests/ai-live.test.mjs` | 真实接口：连接测试、单页生成、多页生成、应用与导出（校验无越界、无真实重叠） |

> 测试文件均为 UTF-8 无 BOM，运行器在任一断言失败时以退出码 1 结束，可直接接入 CI。
## License

This project is licensed under the **MIT License**.

- **Author**: DBmc-666
- **Permission**: 自由使用、复制、修改、合并、发布、分发、再授权及/或销售
- **Conditions**: 必须保留版权声明和本许可声明

See the [LICENSE](./LICENSE) file for full legal text.